apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-ftv-management-interface.fullname" . }}
  labels:
    {{- include "open-ftv-management-interface.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "open-ftv-management-interface.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
