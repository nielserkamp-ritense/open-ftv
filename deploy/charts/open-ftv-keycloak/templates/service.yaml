apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-ftv-keycloak.fullname" . }}
  labels:
    {{- include "open-ftv-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "open-ftv-keycloak.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
