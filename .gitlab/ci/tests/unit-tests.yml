utilities:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/utilities/.go-cache
  before_script:
    - mkdir -p utilities/.go-cache
  script:
    - cd utilities
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - utilities/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - utilities/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

eam-models:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/eam/models/.go-cache
  before_script:
    - mkdir -p eam/models/.go-cache
  script:
    - cd eam/models
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - eam/models/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - eam/models/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - eam/models/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

eam-handlers:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/eam/handlers/.go-cache
  before_script:
    - mkdir -p eam/handlers/.go-cache
  script:
    - cd eam/handlers
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - eam/handlers/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - eam/handlers/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - oas/**/*
          - utilities/**/*
          - utilities-no-ci/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - eam/handlers/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

eam-server:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/eam/server/.go-cache
  before_script:
    - mkdir -p eam/server/.go-cache
  script:
    - cd eam/server
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - eam/server/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - eam/server/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - eam/handlers/**/*
          - eam/server/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

eam-components:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/eam/components/.go-cache
  before_script:
    - mkdir -p eam/components/.go-cache
  script:
    - cd eam/components
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - eam/components/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - eam/components/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

eam-fsc-plugin-generic:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/eam/fsc/plugin/generic/.go-cache
  before_script:
    - mkdir -p eam/fsc/plugin/generic/.go-cache
  script:
    - cd eam/fsc/plugin/generic
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - eam/fsc/plugin/generic/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - eam/fsc/plugin/generic/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - oas/**/*
          - utilities/**/*
          - utilities-no-ci/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - eam/handlers/**/*
          - eam/server/**/*
          - eam/fsc/plugin/generic/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

apps-manager:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/apps/manager/.go-cache
  before_script:
    - mkdir -p apps/manager/.go-cache
  script:
    - cd apps/manager
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - apps/manager/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - apps/manager/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - oas/**/*
          - utilities/**/*
          - utilities-no-ci/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - eam/handlers/**/*
          - eam/server/**/*
          - apps/manager/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

apps-pap:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/apps/pap/.go-cache
  before_script:
    - mkdir -p apps/pap/.go-cache
  script:
    - cd apps/pap
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - apps/pap/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - apps/pap/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - oas/**/*
          - utilities/**/*
          - utilities-no-ci/**/*
          - testdata/**/*
          - eam/models/**/*
          - eam/components/**/*
          - eam/handlers/**/*
          - eam/server/**/*
          - apps/pap/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests
