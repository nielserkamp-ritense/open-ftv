utilities:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/utilities/.go-cache
  before_script:
    - mkdir -p utilities/.go-cache
  script:
    - cd utilities
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - utilities/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - utilities/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

pbac-models:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/pbac/models/.go-cache
  before_script:
    - mkdir -p pbac/models/.go-cache
  script:
    - cd pbac/models
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - pbac/models/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - pbac/models/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - pbac/models/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

pbac-components:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/pbac/components/.go-cache
  services:
    - docker:dind
  before_script:
    - mkdir -p pbac/components/.go-cache
  script:
    - cd pbac/components
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - pbac/components/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - pbac/components/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - pbac/models/**/*
          - pbac/components/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests

pbac-fsc-plugin-generic:
  stage: Tests
  image: golang:1.23
  variables:
    GOPATH: ${CI_PROJECT_DIR}/pbac/fsc/plugin/generic/.go-cache
  services:
    - docker:dind
  before_script:
    - mkdir -p pbac/fsc/plugin/generic/.go-cache
  script:
    - cd pbac/fsc/plugin/generic
    - /usr/local/go/bin/go mod download
    - /usr/local/go/bin/go test ./... -covermode=atomic  -coverprofile cover.out
    - /usr/local/go/bin/go tool cover -html=cover.out -o cover.html
    - /usr/local/go/bin/go tool cover -func=cover.out
    - cd -
  coverage: /total:\t+\(statements\)\t+([\d\.]+?%)/
  cache:
    key: testcache
    paths:
      - pbac/fsc/plugin/generic/.go-cache
  artifacts:
    expire_in: 1 month
    paths:
      - pbac/fsc/plugin/generic/cover.html
  needs: [ ]
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: always
    - if: $CI_COMMIT_BRANCH
      changes:
        compare_to: "main"
        paths:
          - utilities/**/*
          - testdata/**/*
          - pbac/models/**/*
          - pbac/components/**/*
          - pbac/fsc/plugin/generic/**/*
          - .golang-ci.yml
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/ftv-implementatie" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests
