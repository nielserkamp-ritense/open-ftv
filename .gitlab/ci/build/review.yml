build_review_eam:
  stage: Build
  image: quay.io/buildah/stable:v1.44.0
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - pdp
          - manager
          - management-interface
  before_script:
    - export SEMANTIC_RELEASE_NEXT_VERSION=$(cat ci_semantic_release_next_version.txt)
    - export COMPONENT_VERSION="${SEMANTIC_RELEASE_NEXT_VERSION}-review-${CI_COMMIT_SHORT_SHA}"
    - echo "component version=${COMPONENT_VERSION}"
    - echo "container tag=${CI_COMMIT_SHORT_SHA}-${ARCH}"
    - echo -n "${CI_REGISTRY_IMAGE}" > ci_build_image_registry.txt
    - echo -n "${CI_COMMIT_SHORT_SHA}-${ARCH}" > ci_build_image_tag.txt
    - buildah login -u "${CI_REGISTRY_USER}" -p "${CI_REGISTRY_PASSWORD}" "${CI_REGISTRY}"
  script:
    - buildah build
      --platform=linux/${ARCH}
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${COMPONENT_VERSION}
      -f=apps/${APP}/Dockerfile
      -t ${CI_REGISTRY_IMAGE}/eam/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
      .
    - buildah push ${CI_REGISTRY_IMAGE}/eam/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/open-ftv" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests
  artifacts:
    expire_in: 2 weeks
    paths:
      - ci_build_image_registry.txt
      - ci_build_image_tag.txt
  needs:
    - job: unit-tests
      optional: true
    - job: next-version
      optional: false
  resource_group: build_review_eam
  tags:
    - ${ARCH}

build_review_mock_ds:
  stage: Build
  image: quay.io/buildah/stable:v1.44.0
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - generic
  before_script:
    - export SEMANTIC_RELEASE_NEXT_VERSION=$(cat ci_semantic_release_next_version.txt)
    - export COMPONENT_VERSION="${SEMANTIC_RELEASE_NEXT_VERSION}-review-${CI_COMMIT_SHORT_SHA}"
    - echo "component version=${COMPONENT_VERSION}"
    - echo "container tag=${CI_COMMIT_SHORT_SHA}-${ARCH}"
    - echo -n "${CI_REGISTRY_IMAGE}" > ci_build_image_registry.txt
    - echo -n "${CI_COMMIT_SHORT_SHA}-${ARCH}" > ci_build_image_tag.txt
    - buildah login -u "${CI_REGISTRY_USER}" -p "${CI_REGISTRY_PASSWORD}" "${CI_REGISTRY}"
  script:
    - buildah build
      --platform=linux/${ARCH}
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${COMPONENT_VERSION}
      -f=mock/datasources/${APP}/Dockerfile
      -t ${CI_REGISTRY_IMAGE}/mock-ds/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
      .
    - buildah push ${CI_REGISTRY_IMAGE}/mock-ds/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/open-ftv" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests
  artifacts:
    expire_in: 2 weeks
    paths:
      - ci_build_image_registry.txt
      - ci_build_image_tag.txt
  needs:
    - job: unit-tests
      optional: true
    - job: next-version
      optional: false
  resource_group: build_review_mock_ds
  tags:
    - ${ARCH}

build_review_gw_plugin:
  stage: Build
  image: quay.io/buildah/stable:v1.44.0
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - envoy
  before_script:
    - export SEMANTIC_RELEASE_NEXT_VERSION=$(cat ci_semantic_release_next_version.txt)
    - export COMPONENT_VERSION="${SEMANTIC_RELEASE_NEXT_VERSION}-review-${CI_COMMIT_SHORT_SHA}"
    - echo "component version=${COMPONENT_VERSION}"
    - echo "container tag=${CI_COMMIT_SHORT_SHA}-${ARCH}"
    - echo -n "${CI_REGISTRY_IMAGE}" > ci_build_image_registry.txt
    - echo -n "${CI_COMMIT_SHORT_SHA}-${ARCH}" > ci_build_image_tag.txt
    - buildah login -u "${CI_REGISTRY_USER}" -p "${CI_REGISTRY_PASSWORD}" "${CI_REGISTRY}"
  script:
    - buildah build
      --platform=linux/${ARCH}
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${COMPONENT_VERSION}
      -f=external/gateways/${APP}/Dockerfile-plugin
      -t ${CI_REGISTRY_IMAGE}/gw-plugin/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
      .
    - buildah push ${CI_REGISTRY_IMAGE}/gw-plugin/${APP}:${CI_COMMIT_SHORT_SHA}-review-${ARCH}
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
      when: never
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PROJECT_PATH != "digilab.overheid.nl/ecosystem/ftv/open-ftv" && $CI_PIPELINE_SOURCE == "merge_request_event" # run for external fork merge requests
  artifacts:
    expire_in: 2 weeks
    paths:
      - ci_build_image_registry.txt
      - ci_build_image_tag.txt
  needs:
    - job: unit-tests
      optional: true
    - job: next-version
      optional: true
  resource_group: build_review_gw_plugin
  tags:
    - ${ARCH}
