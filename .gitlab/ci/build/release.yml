build_release_eam:
  stage: Build
  image: gcr.io/kaniko-project/executor:v1.23.2-debug@sha256:c3109d5926a997b100c4343944e06c6b30a6804b2f9abe0994d3de6ef92b028e
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - pip
          - pap
          - pdp
          - manager
          - management-interface
  script:
    - /kaniko/executor
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${IMAGE_TAG}
      --context "."
      --dockerfile "apps/${APP}/Dockerfile"
      --single-snapshot
      --cache="true"
      --compressed-caching=false
      --cache-ttl="168h"
      --destination ${CI_REGISTRY_IMAGE}/${APP}:${IMAGE_TAG}-${ARCH}
  rules:
    - if: $CI_COMMIT_TAG && $CI_PROJECT_PATH == "digilab.overheid.nl/ecosystem/ftv/open-ftv"
  tags:
    - ${ARCH}

build_release_mock_ds:
  stage: Build
  image: gcr.io/kaniko-project/executor:v1.23.2-debug@sha256:c3109d5926a997b100c4343944e06c6b30a6804b2f9abe0994d3de6ef92b028e
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - generic
          - vlierdam
          - rvig
          - rdw
  script:
    - /kaniko/executor
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${IMAGE_TAG}
      --context "."
      --dockerfile "mock/datasources/generic/${APP}.Dockerfile"
      --single-snapshot
      --cache="true"
      --compressed-caching=false
      --cache-ttl="168h"
      --destination ${CI_REGISTRY_IMAGE}/mock-ds-${APP}:${IMAGE_TAG}-${ARCH}
  rules:
    - if: $CI_COMMIT_TAG && $CI_PROJECT_PATH == "digilab.overheid.nl/ecosystem/ftv/open-ftv"
  tags:
    - ${ARCH}

build_release_gw_plugin:
  stage: Build
  image: gcr.io/kaniko-project/executor:v1.23.2-debug@sha256:c3109d5926a997b100c4343944e06c6b30a6804b2f9abe0994d3de6ef92b028e
  parallel:
    matrix:
      - ARCH:
          - amd64
        APP:
          - envoy
  script:
    - /kaniko/executor
      --build-arg GIT_COMMIT_HASH=${CI_COMMIT_SHA}
      --build-arg GIT_TAG_NAME=${IMAGE_TAG}
      --context "."
      --dockerfile "external/gateways/${APP}/Dockerfile-plugin"
      --single-snapshot
      --cache="true"
      --compressed-caching=false
      --cache-ttl="168h"
      --destination ${CI_REGISTRY_IMAGE}/gw-plugin-${APP}:${IMAGE_TAG}-${ARCH}
  rules:
    - if: $CI_COMMIT_TAG && $CI_PROJECT_PATH == "digilab.overheid.nl/ecosystem/ftv/open-ftv"
  tags:
    - ${ARCH}
