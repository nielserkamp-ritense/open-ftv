---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: "https://inway-fsc-nlx-inway:443/brp-personen"
  version: "default"
  rules:
    - actions:
        - "*"
      effect: EFFECT_ALLOW
      roles:
        - doelbinding
