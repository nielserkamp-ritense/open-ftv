---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  resource: "https://inway-fsc-nlx-inway:443/brp-personen"
  version: "default"
  constants:
    import:
      - rvig
  rules:
    - actions: ['POST']
      effect: EFFECT_ALLOW
      roles: ['*']
